{
    "name": "heroku-multi-buildpack-app",
    "version": "1.0.0",
    "scripts": {
      "heroku-postbuild": "cd frontend && npm install && npm run build && cp -r dist/* ../backend/static/"
    }
  }